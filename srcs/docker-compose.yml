services:
  nginx:
    build: ./requirements/nginx
  mariadb:
    build: ./requirements/mariadb
    volumes:
      - DB:/var/lib/mysql/
  wordpress:
    build: ./requirements/wordpress
    volumes:
      - wpdata:/data/
volumes:
  DB:
  wpdata: 